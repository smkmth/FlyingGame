Every game needs an ObjectPooler object, a ScreenManager object- carefull of the names they are checked.

You also need a tag 'bullet'

The ObjectPooler needs to be inited with a prefab of an enemey, a player and a bullet - in prefabs folder.

